# Redis Development Configuration
# Disables persistence for development/testing

# Disable RDB snapshots
save ""

# Disable AOF (Append Only File)
appendonly no

# Allow writes even if persistence fails
stop-writes-on-bgsave-error no